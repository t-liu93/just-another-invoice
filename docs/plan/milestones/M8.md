# M8 · Expenses (incl. AI Receipt Recognition)

> 🌐 **English** · [中文](M8_zh.md)

> JIT output before entering this milestone (**frozen · 2026-06-13 finalized with author D1–D16**; includes AI client (httpx self-constructed) / configurable prompt / PDF rasterization — three rounds of additions). Read `docs/plan/roadmap.md` §2 global constraints + M8 entry; authority analysis doc sections: §7.1.B (payment & expense v1 scope), §7.4.1 (input net + VAT split storage), §7.4.2 / §7.4.7 (input-side VAT categories & reporting destination), §7.4.4 (each expense required fields), §3.5 (three-layer settings), §3.6 (files / storage / scheduled jobs).
>
> **This milestone direction**: Add a **completely invoice-decoupled expense line** to the system — `expense` entity hanging from `company_id` (input net + VAT split storage, deductible flag), receipt uploads via disk storage abstraction, APScheduler-driven recurring expense auto-generation, plus a **vision LLM receipt recognition pipeline** (photograph receipt → prefill → manual confirm before persist). **All money-math backend, single base currency** (same as M5/M7).

## Adjacent Capabilities Already in Place (Read Before Starting — Don't Reinvent)
> These all landed in M1–M7; M8 directly reuses / mirrors. Each item has a file landmark.

- **Input VAT categories (M4 seeded, reuse direct, no separate enum · red line 12)**: `models/vat.py::VatTreatment` + `services/vat.py::_TREATMENT_SEEDS` already seeded **4 PURCHASE-side** treatments with `effect` and `deductible` hint columns:
  - `NL_DOMESTIC_PURCH` (`APPLY_RATE`, `deductible=True`) → domestic normal input VAT (M10 → 5b);
  - `EU_B2B_REVERSE_PURCH` (`ZERO_REVERSE`, `requires_icp=True`, `deductible=True`) → EU-internal purchase self-assessment (M10 → 4b output + 5b input offset, §7.4.7);
  - `EU_B2C_PURCH` (`APPLY_RATE`);
  - `IMPORT_NON_EU` (`APPLY_RATE`) → non-EU import (M10 → 4a).
  - `report_box` still NULL (to be filled when M10 decides with author); M8 only **stores category + net + VAT correctly**, **no reporting aggregation** (aggregation in M10).
- **Deductible default source (M4 built column)**: `models/dictionary.py::ExpenseCategory.default_deductible` (nullable bool) → each expense `deductible` default value source (can override per line).
- **VAT rate dictionary**: `models/vat.py::VatRate` (`percent` `NUMERIC(6,3)`, user-changeable).
- **Receipt storage ≠ `binary_asset`**: `models/binary_asset.py` is 512 KB limit **bytea** (for logo); its docstring explicitly states "**receipts use M8 disk storage + storage abstraction**"; roadmap M8 also says "local storage + storage abstraction". **This milestone creates disk storage abstraction + `expense_attachment` metadata table; don't stuff into `binary_asset`.** Upload validation mirrors `services/assets.py` (MIME whitelist + size limit + path safety, red line 7).
- **APScheduler (M6 established)**: Single `AsyncIOScheduler` in `main.py::lifespan`, already has `_expire_quotes_job` (cron, calls `services/quote.expire_due_quotes_all`; doesn't self-commit; job wraps its own transaction). **Recurring expense generation = add second cron job on same scheduler**, mirror this pattern; config flags see `config.py::scheduler_enabled` / `scheduler_expire_quotes_hour`.
- **Three-layer settings + credential storage pattern (M1/M2 established)**: `services/settings.py` (`get_setting`/`set_setting`, `USER→COMPANY→GLOBAL` fallback + in-process cache). **SMTP credential pattern** (`schemas/setting.py::SmtpSettings`/`SmtpSettingsRead` + `api/settings.py` `GET/PUT /settings/smtp` + `services/email.py::_get_smtp_config` "DB settings → env fallback") = **AI credential exactly mirrors this template** (plain JSONB store, read-time masking, env fallback).
- **Money rounding (M0/M7.5 established)**: `services/money.py::quantize_money` (3-digit intermediate precision) / `quantize_to_minor_unit` (2-digit / cents, document level). Expense amounts are **direct-entry to-cents amounts** (receipt/AI gives cents-included net/VAT); don't go through M5's line→discount→tax pipeline; persist to cents (`quantize_to_minor_unit`), column still `NUMERIC(18,3)`.
- **Mirror templates**: Entity / service / route layering + cascade aligned with `models/invoice.py` + `models/payment.py` (`_MONEY = Numeric(18,3)`, `company_id` `RESTRICT`, child tables `CASCADE`, dict FK `SET NULL` + name snapshot, `creator_id` `SET NULL`); list / pagination / filter mirrors `services/payment.py::list_payments` + `services/invoice.py`; migration pattern aligns with M6.5/M7 additive migrations in `backend/alembic/versions/`.

## Execution Model (Two Implementation Modes · Protocol Fixed in CLAUDE.md, Not Repeated Here)
> Same as `M7.md` top section: ① Opus orchestrator → clean Sonnet implementer (empty conversation) → Chinese impl report → clean Opus blind review → Sonnet fixer loop until zero findings → per-step autosquash → milestone-end report for author manual walkthrough; ② author manual implement each step + agent review. **Default manual mode**; orchestrator mode only if author explicitly requests.
> **Contract for both modes**: Each atomic step **self-contained** — a clean agent with only "this doc + repo (CLAUDE.md / memory / existing code) + brief author remarks" can land it; each step gives clear **goal / contract / existing files to mirror / invariants / required test coverage / blind review points**; progressive safety net = automated tests + blind review; manual walkthrough converges to **one final time at milestone end** (see "🟢 Deployment Self-Test Points" at bottom).

## Goals and Scope
- **Goal**: Record one expense (input net + VAT split, VAT treatment category, deductibility); upload/preview/download receipt; auto-generate recurring fixed costs (generated as drafts awaiting confirm); photograph receipt with vision LLM to prefill fields, manual confirm before persist. **All money-math backend, single base currency.**
- **Dependencies**: M2 (company base currency) + M4 (`expense_category` / `payment_method` as needed / `vat_treatment` PURCHASE-side / `vat_rate` / currencies). **Not dependent on invoices/quotes/payments** — roadmap §4 "expense independent line", parallel to M5/M6/M7 document lines; project already completed M5–M7, M8 starts now.
- **In Scope (IN)**:
  - **Expense entity + CRUD + list**: Hang from `company_id` (red line 2); category (M4, FK `SET NULL` + name snapshot); supplier (v1 = free text `supplier_name`); **net + input VAT split** (§7.4.1, both authority entry values; `gross = net + vat` calculated by backend); VAT treatment category (PURCHASE-side `vat_treatment`, FK `RESTRICT` + code/label/effect snapshot) + rate (`vat_rate`, FK `RESTRICT` + percent/label snapshot); **deductibility** (default from category `default_deductible`, per-line override); expense date; `note` (`text`); `reference` (`text`, receipt number); `is_draft` (for recurring generation). **Single base currency**: `currency = company base_currency`, `exchange_rate = 1`, `base_* = original value`.
  - **Receipt upload (disk storage abstraction)**: Create `services/storage.py` (`Storage` abstraction + `LocalStorage` implementation, configurable root path + size limit + MIME whitelist, path traversal prevention) + `expense_attachment` metadata table (`CASCADE` to expense); upload / list / inline download / delete. **Storage abstraction reserved for shared use by M9 PDF**.
  - **Recurring expenses**: `recurring_expense` template + frequency (MONTHLY/QUARTERLY/YEARLY) → APScheduler periodically **idempotent clone** `is_draft=true` expenses (reuse M6 scheduler); pausable (`active`); can set limits (count or end date); provide "run now" manual trigger endpoint (for walkthrough / backfill).
  - **⭐ AI Receipt Smart Fill (OpenAI-compatible / Chat Completions)**: `services/ai.py` vision pipeline via **OpenAI-compatible Chat Completions interface** — `base_url` / `api_key` / `model` / `receipt_prompt` **all user-configured in settings** (can point to OpenAI / OpenRouter / local vLLM or any compatible endpoint); client uses **`httpx` (async) self-constructs requests, no vendor SDK** (direct `POST {base_url}/chat/completions` + multi-modal `image_url`). **Don't hardcode any provider / model id.** **Synchronous call**; `POST /expenses/ai-extract` (against one **already-uploaded-to-storage receipt**) → returns `ExpenseAIPrefill` (net/VAT/rate/supplier/date/category suggestion) **never persists to DB**; manual confirm in editor before proper expense save; **failure / AI off → graceful fallback to hand-fill**. **Normalize input to images** (D16): images send direct; PDF (electronic/scanned) backend rasterizes with `pypdfium2` to image then sends — three cases, one path. Credentials via M2 three-layer settings (**mirror SMTP pattern**: plain global JSONB + read-time mask + env fallback, red lines 5/9) + "enable AI" switch; **prompt configurable** (default + user override); **credentials never in logs / never in any response body**.
  - **AI connectivity + multi-modal test**: `POST /settings/ai/test` (**mirrors `POST /settings/smtp/test`**) — backend takes config (already stored or temp in request body) and **actually sends one small-image Chat Completions probe**, verifying ① connects / auth passes; ② model **accepts image input (multi-modal)**; returns `{ ok, multimodal, detail }`. Frontend settings page has "test" button.
  - **Frontend**: expense list (filters) / editor / receipt preview (raw image `<img>` / PDF embed) / recurring management / "photograph receipt auto-fill" entry; gear settings panel adds **AI category** (fill `base_url`/`model`/`api_key` + enable switch + **"test" button**, reuse M2.5 "future AI expansion" slot); i18n EN/ZH. **Frontend doesn't locally authority-calculate** (`gross` etc. from backend).
- **Out of Scope / Deferred (OUT)**:
  - **⭐ AI supplier price-list recognition → fill M4 product catalog** (follow-on) → **M8 main-line close then single separate round** (author 2026-06-13 decision). Reuses this milestone's vision pipeline, upserts by SKU into `product` (see `product-sku-upsert`); **not counted in M8 acceptance**; "follow-on" section at end gives pointers.
  - **At-rest credential encryption** → Not doing (mirror SMTP plain pattern; repo has no crypto foundation; stay isomorphic).
  - **Server-side thumbnail generation** → Not doing (serve original; frontend renders preview). Note: `Pillow` brought in by PDF rasterization (D16) but **not used for thumbnails**.
  - **Native PDF input to LLM / extract PDF text layer feed LLM** → Not doing. OpenAI-compatible Chat Completions `image_url` only accepts images; native PDF is vendor-private extension (binds provider, violates base_url any-compatible-endpoint premise); text extraction only valid for electronic PDFs and scrambles receipt layout. **Switch to backend rasterize-to-image uniform path** (see D16, in IN).
  - **Multi-currency expenses / expense-side FX / payment-date exchange rate** → Deferred (same single-base-currency stance as document line, §7.4.5 cash-basis later). Build `base_*` and `exchange_rate` columns as placeholders; additive later.
  - **Partial deductible % (entertainment etc., §7.4.4)** → Deferred (v1 `deductible` boolean).
  - **Input VAT reporting aggregation** (2a/4a/4b/5b) → M10 (M8 only **stores net/VAT/category/deductible correctly**).
  - **Supplier as independent entity / hang from M3 customer** → Deferred (v1 supplier = free text).
  - **Bank statement auto-import expenses** → vNext (roadmap §4.x).
  - **Expense duplication** → Deferred (roadmap marked 🔜).
- **Corresponding docs**: roadmap M8 / §2 (**red lines 1 / 2 / 3 / 5 / 7 / 9 / 10 / 12**); analysis doc §7.1.B / §7.4.1 / §7.4.2 / §7.4.4 / §7.4.7 / §3.5 / §3.6.

## Product and Technical Decisions for This Round (Finalized with Author Before Starting · 2026-06-13)
- [x] **D1 · AI credential storage = mirror SMTP pattern**: `SETTING_KEY_AI = "ai"`, GLOBAL level, `AiSettings { enabled: bool, base_url: str, api_key: str, model: str }` stored plain in JSONB; read returns `AiSettingsRead { enabled, base_url, api_key_set: bool, model }` masked (only `api_key` hidden); `services/ai.py` follows "DB settings → env fallback (`config.ai_base_url` / `ai_api_key` / `ai_model` / `ai_enabled`)". **No at-rest encryption** (isomorphic with existing SMTP). `base_url` default `https://api.openai.com/v1`, user-overridable.
- [x] **D2 · Receipts = disk storage abstraction, no thumbnails**: Create `services/storage.py` (`LocalStorage`, root `config.storage_root`, size limit `config.max_receipt_bytes`, MIME whitelist = images + PDF), `expense_attachment` stores metadata + storage key; **don't go into `binary_asset` bytea**; no thumbnails (serve original file). Share storage interface with M9 PDF.
- [x] **D3 · Recurring expense generation as draft awaiting confirm**: Scheduled job clones `is_draft=true` expenses; user confirms / attaches receipt in list to commit (`is_draft=false`). **Idempotent** (generate once per period), pausable, can set limits (count / end date).
- [x] **D4 · AI price-list follow-on is a separate standalone round**: M8 main line (expense CRUD + receipts + recurring + AI receipt fill + frontend closure) closes and passes acceptance first; price-list → catalog recognition reuses AI pipeline as a separate post-M8 round (not counted in M8 acceptance).
- [x] **D5 · AI call shape = synchronous + prefill no-persist + failure fallback hand-fill**: `POST /expenses/ai-extract` synchronously calls vision model, returns `ExpenseAIPrefill` for frontend preview only; user confirms then normal `POST /expenses` persists. AI off / call failure → return clear signal; frontend falls back to manual fill.
- [x] **D6 · AI client = OpenAI-compatible Chat Completions, self-constructed with `httpx`, no vendor SDK (author decided 2026-06-13)**: **Don't use `anthropic`, don't use `openai` SDK, don't use `requests` (sync blocks async), don't hardcode any provider/model**; `base_url`/`api_key`/`model` all user-configured in settings (can point to OpenAI / OpenRouter / local vLLM etc.). Client directly `httpx.AsyncClient` `POST {base_url}/chat/completions`, request/response use custom small Pydantic models; `httpx` promoted from dev to **main dependency**. **Calls must be injectable / mockable** (`respx`/`MockTransport`); unit tests don't hit real network. Multi-modal via `image_url` (base64) — **images send directly; PDF backend rasterizes then sends** (see D16), three receipt types one path. Rationale: call surface tiny and stable, async-native, provider-agnostic = HTTP contract not SDK, one fewer churn-prone dependency.
- [x] **D14 · AI settings include connectivity + multi-modal test** (author decided 2026-06-13): `POST /settings/ai/test` mirrors SMTP test — backend uses config `httpx` to actually send one small-image Chat Completions probe, returns `{ ok, multimodal, detail }`, confirming connectivity + model accepts image input; frontend settings page has "test" button. **Probe image is built-in tiny image (no user data sent externally); key not in response/logs.**
- [x] **D15 · Prompt configurable (default + user override · author decided 2026-06-13)**: `AiSettings.receipt_prompt` (GLOBAL, user-editable in settings); empty → falls back to code constant `DEFAULT_RECEIPT_PROMPT` (includes Dutch receipt/BTW context). **User only edits the "extraction instructions"; output JSON field contract is system-fixed appended and non-editable** (`_OUTPUT_CONTRACT` footer), keeping parsing robust; parser fully fault-tolerant (`ExpenseAIPrefill` all fields nullable). Model returns only **category text name**; service side best-effort matches by name to company's `category_id`; don't let model guess VAT treatment (leave to user, default `NL_DOMESTIC_PURCH`). No migration (uses existing `setting` JSONB).
- [x] **D16 · PDF receipts = backend rasterize to image, uniform image_url path (author decided 2026-06-13 · reverses earlier "PDF→AI deferred")**: `ai-extract` **normalizes all inputs to images** before feeding model — `image/*` use directly; `application/pdf` use **`pypdfium2`** (self-contained pre-compiled wheel, **no system poppler**, BSD/Apache license) page-by-page `render()` → `Pillow` encode PNG/JPEG → base64 into `image_url` (each page one part; page limit `config.ai_pdf_max_pages` default 3; render scale/DPI configurable). **Electronic PDF and scanned PDF treated identically** (scanned rasterization just retrieves that scanned image). **Rationale**: `image_url` is the only provider-agnostic path; model "sees" image better than text extraction, doesn't lose layout; one path / one prompt / one parser. New deps `pypdfium2` + `Pillow` (pure wheel, no system packages; not `pdf2image` [needs poppler] / `PyMuPDF` [AGPL]). Genuinely unsupported MIME → `422`.
- [x] **D7 · Input VAT reuses `vat_treatment` (PURCHASE-side) + `vat_rate`**: No separate expense-side enum (red line 12). Treatment FK `RESTRICT` + snapshots code/label/effect; rate FK `RESTRICT` + snapshots percent/label (mirrors `invoice`).
- [x] **D8 · Net + VAT both authority entry values, no forced `vat == net × rate`**: Real receipts round each line independently; totals commonly differ from `net×rate` by a few cents. Therefore store net and VAT both exactly **as-given** (receipt/AI gives to-cents values); backend only calculates `gross = net + vat` then quantizes to cents; **no equality validation** (`vat_rate` only serves as reporting category / display reference, not calculation driver).
- [x] **D9 · Deductible default from category, per-line overridable**: `ExpenseInput.deductible` omitted → use `expense_category.default_deductible` (further fallback → `true`); explicit value takes precedence.
- [x] **D10 · Single base currency** (same as M5/M7): `currency = company base_currency`, `exchange_rate = 1`, `base_net/base_vat/base_gross = original values`; non-base-currency amounts rejected by backend (`422`). FX placeholder additive later.
- [x] **D11 · All amounts `Decimal`/`NUMERIC(18,3)`, persisted to cents** (red line 1 + M7.5): Entry net/vat `quantize_to_minor_unit` (EUR=2 digits) before persisting; `gross = quantize_to_minor_unit(net + vat)`. **Money-math logic must have unit tests.**
- [x] **D12 · Category FK `SET NULL` + name snapshot; delete expense cascades delete attachments + disk files** (red line 3): `expense.category_id` `SET NULL` (nullable; required by business at create time; name snapshot preserves history after dict item deleted); `expense_attachment.expense_id` `ON DELETE CASCADE`; plus **service deletes disk files when deleting expense** (DB cascade only clears metadata rows).
- [x] **D13 · AI settings UI in gear panel**: Reuse M2.5 "future AI expansion" slot (gear → AI category); don't create new entry.

## Contract (Upfront · Frontend and Backend Write Against This Independently)
> All business endpoints `/api/v1/*`. Changing the contract → `npm run codegen` regenerates `schema.d.ts`; CI drift check forces no-drift (red line 11). Reuse M1 cookie session + `current_mfa_user`; owner-only reuses `api/payments.py::_owner_only` / `api/invoices.py::_owner_only` pattern. All write endpoints inject `company_id` from service; never from request body.

**Expense CRUD / List (Step 1)**
- `POST /api/v1/expenses` body `ExpenseInput` → `201 ExpenseRead` (non-base-currency → `422`; category/treatment/rate cross-company → `404`/`422`).
- `GET /api/v1/expenses` query `{q?, category_id?, vat_treatment_id?, deductible?: bool, is_draft?: bool, date_from?, date_to?, limit?=50, offset?=0, sort_by?: "expense_date"|"created_at"="expense_date"}` → `200 ExpenseListResponse {items: ExpenseListItem[], total}`.
- `GET /api/v1/expenses/{id}` → `200 ExpenseRead`; cross-company → `404`.
- `PUT /api/v1/expenses/{id}` body `ExpenseInput` → `200 ExpenseRead` (includes setting `is_draft` from true to false to "confirm" a recurring draft).
- `DELETE /api/v1/expenses/{id}` → `204` (cascade delete `expense_attachment` rows + service deletes disk files).

**Receipt Attachments (Step 2)**
- `POST /api/v1/expenses/{id}/attachments` (`multipart/form-data`, field `file`) → `201 ExpenseAttachmentRead` (MIME/size invalid → `422`; cross-company expense → `404`).
- `GET /api/v1/expenses/{id}/attachments` → `200 ExpenseAttachmentListResponse`.
- `GET /api/v1/attachments/{attachment_id}/content` → `200` original file stream (`Content-Type` = stored MIME, `Content-Disposition: inline`); cross-company → `404`.
- `DELETE /api/v1/attachments/{attachment_id}` → `204` (delete DB row + disk file).

**Recurring Expenses (Step 3)**
- `POST /api/v1/recurring-expenses` body `RecurringExpenseInput` → `201 RecurringExpenseRead`.
- `GET /api/v1/recurring-expenses` query `{active?: bool, limit?, offset?}` → `200 RecurringExpenseListResponse`.
- `GET /api/v1/recurring-expenses/{id}` → `200 RecurringExpenseRead`; cross-company → `404`.
- `PUT /api/v1/recurring-expenses/{id}` body `RecurringExpenseInput` → `200 RecurringExpenseRead` (includes `active` pause/resume).
- `DELETE /api/v1/recurring-expenses/{id}` → `204` (**don't** cascade delete generated historical expenses — they are independent records).
- `POST /api/v1/recurring-expenses/{id}/run-now` → `200 { generated: int, next_run_date }` (manually trigger one generation cycle, same idempotency as scheduled job; for walkthrough / backfill).

**AI Receipt Fill + Settings (Step 4)**
- `POST /api/v1/expenses/ai-extract` body `{ attachment_id: uuid }` → `200 ExpenseAIPrefill` (recognizes already-uploaded receipt — **images used directly; PDFs backend-rasterized to image**, D16); genuinely unsupported MIME → `422`; AI off / no key → `409` / clear error code; model failure/timeout → `502` + recoverable signal; cross-company attachment → `404`.
- `GET /api/v1/settings/ai` → `200 AiSettingsRead` (**masked**: only returns `enabled` / `base_url` / `api_key_set` / `model`).
- `PUT /api/v1/settings/ai` body `AiSettingsUpdate { enabled?, base_url?, api_key?, model? }` → `200 AiSettingsRead` (`api_key` omitted = retain existing; empty string = clear; mirrors `SmtpSettingsUpdate` semantics).
- `POST /api/v1/settings/ai/test` body `AiSettingsUpdate` (omitted fields fall back to stored config) → `200 AiTestResult { ok: bool, multimodal: bool, detail: str }` (**mirrors `POST /settings/smtp/test`**; backend sends small built-in image Chat Completions probe, verifies connectivity + multi-modal; **response/logs don't contain key**).

**Core Schema Shapes** (in `schemas/expense.py` + `schemas/setting.py`)
- `ExpenseInput { expense_date: date, category_id: uuid, supplier_name?: text, vat_treatment_id: uuid, vat_rate_id: uuid, net_amount: Decimal(≥0), vat_amount: Decimal(≥0), deductible?: bool, reference?: text, note?: text }`
  - Validation: `net_amount`/`vat_amount` ≥ 0; `reference`/`note`/`supplier_name` are `text` (red line 10). **Only receives raw inputs; doesn't accept `gross`/`base_*`/`is_draft`/snapshot fields** (money-math and snapshots in backend, red line 1).
- `ExpenseRead { id, expense_date, category_id?, category_name?, supplier_name?, vat_treatment_id?, vat_treatment_code, vat_treatment_label, vat_treatment_effect, vat_rate_id?, vat_rate_percent, vat_rate_label, net_amount, vat_amount, gross_amount, deductible, currency, exchange_rate, base_net_amount, base_vat_amount, base_gross_amount, reference?, note?, is_draft, recurring_expense_id?, attachment_count, created_at, updated_at }`.
- `ExpenseListItem { id, expense_date, category_name?, supplier_name?, net_amount, vat_amount, gross_amount, deductible, is_draft, attachment_count }`.
- `ExpenseListResponse { items: ExpenseListItem[], total }`.
- `ExpenseAttachmentRead { id, expense_id, filename, mime_type, byte_size, created_at }` (**don't expose storage key / disk path**).
- `ExpenseAttachmentListResponse { items: ExpenseAttachmentRead[] }`.
- `RecurringExpenseInput { name: text, category_id, supplier_name?, vat_treatment_id, vat_rate_id, net_amount, vat_amount, deductible?, note?, frequency: "MONTHLY"|"QUARTERLY"|"YEARLY", start_date: date, end_date?: date, max_occurrences?: int, active?: bool=true }`.
- `RecurringExpenseRead { ...input echoed..., id, next_run_date, occurrences_generated, last_generated_at?, active, created_at, updated_at }`.
- `RecurringExpenseListResponse { items, total }`.
- `ExpenseAIPrefill { expense_date?: date, supplier_name?: str, net_amount?: Decimal, vat_amount?: Decimal, vat_rate_percent?: Decimal, suggested_category_name?: str, suggested_category_id?: uuid, raw_model_note?: str, confidence?: str }` (**all nullable** — model can't recognize → leave empty; frontend fills what it can; no credentials. Model returns **category text name** `suggested_category_name`; service best-effort matches name to company's `suggested_category_id`; VAT treatment not in prefill, left for user to pick).
- `AiSettings { enabled: bool=false, base_url: str="https://api.openai.com/v1", api_key: str="", model: str="", receipt_prompt: str="" }` (GLOBAL, plain JSONB; `receipt_prompt` empty → fall back to `DEFAULT_RECEIPT_PROMPT`); `AiSettingsRead { enabled, base_url, api_key_set: bool, model, receipt_prompt }`; `AiSettingsUpdate { enabled?, base_url?, api_key?, model?, receipt_prompt? }`; `AiTestResult { ok: bool, multimodal: bool, detail: str }`. Mirrors `SmtpSettings`/`SmtpSettingsRead`/`SmtpSettingsUpdate`.

## Money-Math and Split-Storage Rules (M8 Locked · `services/expense`)
> Schema only validates shape; all amounts / snapshots / defaults calculated by service. **Unrelated** to M5 tax engine (expenses don't recalculate tax; net/VAT stored as given on receipt).

- **Split storage (§7.4.1)**: `net_amount` and `vat_amount` both **authority entry values**; each `quantize_to_minor_unit` (EUR=2 digits, D11) before persist. **No validation that `vat == net × rate`** (D8).
- **Total**: `gross_amount = quantize_to_minor_unit(net_amount + vat_amount)`.
- **Deductible default (D9)**: `deductible` omitted → `expense_category.default_deductible`; further fallback → `True`; explicit value takes precedence.
- **Snapshots (D7/D12)**: At persist time snapshot `category_name` (FK `SET NULL`), `vat_treatment_code/label/effect` (FK `RESTRICT`), `vat_rate_percent/label` (FK `RESTRICT`) — deleting dict items doesn't corrupt history.
- **Single base currency (D10)**: `currency = company.base_currency`, `exchange_rate = 1`, `base_net_amount=net_amount`, `base_vat_amount=vat_amount`, `base_gross_amount=gross_amount`; request implying non-base-currency → `422`.
- **Validation**: `net/vat ≥ 0`; `category_id`/`vat_treatment_id`/`vat_rate_id` must belong to this company and `vat_treatment.side == PURCHASE` (SALE-side treatment used for expense → `422`).

## Storage Abstraction (M8 Locked · `services/storage.py` · Shared with M9)
> Red line 7 (path traversal prevention / input sanitization) + red line 9 (don't open inbound control endpoints).

- **Interface** (minimal set, reusable by M9 PDF): `save(namespace: str, key: str, content: bytes, content_type: str) -> str` (returns opaque storage key) / `open(key) -> bytes` (or stream) / `delete(key)` / `exists(key)`.
- **`LocalStorage` implementation**: Root = `config.storage_root` (container default `/data/storage`; dev default `./var/storage`; **separate volume from DB / frontend build artifacts**); layout `receipts/{company_id}/{expense_id}/{attachment_id}.{ext}`; **normalize and validate path is under root before write** (reject `..` traversal); filename uses only controlled UUID + whitelist extension; **don't trust client filename** (store display-only in DB).
- **Upload validation** (mirrors `services/assets.py`): MIME whitelist = `image/png`, `image/jpeg`, `image/webp`, `application/pdf`; size limit `config.max_receipt_bytes` (default 10 MB); double-check by **actual magic bytes / Content-Type**, reject spoofed.
- **Delete consistency**: When deleting attachment / expense, service calls `storage.delete(key)` in same logical unit as DB row delete; disk delete failure logs but doesn't block DB transaction (orphan files handled by later cleanup; DB consistency takes priority).

## AI Receipt Pipeline (M8 Locked · `services/ai.py` · OpenAI-Compatible Chat Completions)
> Red line 5 (typed settings for credentials) + red line 9 (don't open self-update/control endpoints) + red line 7 (treat receipt images as untrusted input).

- **Client (D6) = `httpx` (async), no vendor SDK**: Direct `POST {base_url}/chat/completions` (OpenAI-compatible contract); messages body includes text + multi-modal `image_url` (base64 data URL); reads `choices[0].message.content`. **`base_url`/`api_key`/`model` all from config; don't hardcode any provider/model id.** Custom small Pydantic models define **request body + response subset** (mypy-friendly); **HTTP calls injectable / mockable** (service accepts `httpx.AsyncClient` or factory); unit tests use `respx`/`MockTransport`, **never hit real network**. Timeout/error mapping self-controlled (connection failure → `502` + recoverable signal). **Rationale**: call surface tiny and stable, async-native (httpx already in stack), provider-agnostic = HTTP contract not SDK, one fewer churn-prone dependency.
- **Credentials (D1)**: `_get_ai_config(session)` = DB `SETTING_KEY_AI` → env fallback (`config.ai_base_url`/`ai_api_key`/`ai_model`/`ai_enabled`), mirrors `services/email.py::_get_smtp_config`. **Key never in logs, never in any response body / `ExpenseAIPrefill` / `AiTestResult`**; `AiSettingsRead` only returns `api_key_set: bool`.
- **Prompt (D15 · configurable)**: Final user-message text sent to model = `(settings.receipt_prompt or DEFAULT_RECEIPT_PROMPT)` + **system-fixed appended output contract footer** (`_OUTPUT_CONTRACT` constant, enumerating JSON keys our parser depends on, requiring "only return one JSON object; omit keys you can't recognize"). **User can only edit the extraction instructions part; output JSON contract is non-editable** — so changing prompt can't break parsing. `DEFAULT_RECEIPT_PROMPT` is a repository constant (includes Dutch receipt/BTW context hints). Optional: include `response_format={"type":"json_object"}` if endpoint supports it, but don't hard-depend (parser is fault-tolerant anyway).
- **Normalize input to images (D16)**: `_attachment_to_images(attachment, bytes) -> list[image_bytes]` — `image/*` returns `[bytes]` directly; `application/pdf` uses `pypdfium2` page-by-page `render(scale=…)` → `Pillow` encode (max `config.ai_pdf_max_pages` pages); other MIME → `422`. **Electronic and scanned PDFs treated identically**.
- **Extraction flow (D5)**: `extract_from_attachment(session, company_id, attachment_id)` → get this company's attachment (cross-company `404`) → if AI `enabled=false` / no key → clear error (frontend falls back to manual fill, don't raise 500) → read storage file → `_attachment_to_images` (non-image non-PDF → `422`) → each image as base64 into one `image_url` part + effective prompt → call model → **fault-tolerant parse** (strip code fence, `json.loads`, best-effort convert each field to `Decimal`/`date`, ignore unknown keys, **all fields nullable**) → service side best-effort match model's returned category text to company `expense_category.id` (no match → return text only) → compose `ExpenseAIPrefill` (parse failure/timeout → `502` + recoverable signal) → **return; don't persist**. Don't let model guess VAT treatment (cross-border classification unreliable); leave to user, default `NL_DOMESTIC_PURCH`.
- **Test probe (D14)**: `test_ai_config(cfg)` → use config `httpx` to send one **built-in tiny image** (fixed few-dozen-byte PNG in repo, **no user data sent**) + short text (e.g. "reply OK") Chat Completions request → normal response ⇒ `{ok:true, multimodal:true}`; auth/connection failure, model not found, model rejects image ⇒ `ok`/`multimodal` set false accordingly + `detail` reason. Called by `POST /settings/ai/test`.
- **Security**: Treat receipt images as untrusted; validate range/type when parsing model's returned text/numbers; don't directly stuff unfiltered model output into rendering contexts (red line 7; PDF in M9, but prefill text into frontend goes through Vue's default escaping).

## Data Model / Migrations
> UUID PK; root hanging from `company_id` (red line 2, `RESTRICT`); child FK `CASCADE` (red line 3); dict FK `SET NULL`/`RESTRICT` + snapshots; money columns `NUMERIC(18,3)`; text columns `text` (red line 10). **Three new tables** (`expense` / `expense_attachment` / `recurring_expense`) + one new enum `RecurringFrequency`; **don't touch existing tables**. Can split into three additive migrations (step 1 one: expense; step 2 one: expense_attachment; step 3 one: recurring_expense), each step carries its own.

- **`expense`**: `id`; `company_id` FK→`company.id`(`RESTRICT`) index; `expense_date` Date NOT NULL index; `category_id` FK→`expense_category.id`(`SET NULL`) nullable index; `category_name` text nullable (snapshot); `supplier_name` text nullable; `vat_treatment_id` FK→`vat_treatment.id`(`RESTRICT`) nullable; `vat_treatment_code`/`vat_treatment_label`/`vat_treatment_effect` text NOT NULL (snapshots); `vat_rate_id` FK→`vat_rate.id`(`RESTRICT`) nullable; `vat_rate_percent` `NUMERIC(6,3)` NOT NULL (snapshot); `vat_rate_label` text NOT NULL (snapshot); `net_amount`/`vat_amount`/`gross_amount` `NUMERIC(18,3)` NOT NULL; `deductible` Boolean NOT NULL; `currency` String(3) NOT NULL; `exchange_rate` `NUMERIC(18,8)` NOT NULL server_default `1`; `base_net_amount`/`base_vat_amount`/`base_gross_amount` `NUMERIC(18,3)` NOT NULL; `reference` text nullable; `note` text nullable; `is_draft` Boolean NOT NULL server_default `false`; `recurring_expense_id` FK→`recurring_expense.id`(`SET NULL`) nullable (marks source; delete template doesn't delete historical expenses); `creator_id` FK→`user.id`(`SET NULL`) nullable; timestamps. Indexes: `ix_expense_company_id` / `ix_expense_expense_date` / `ix_expense_category_id`.
- **`expense_attachment`**: `id`; `company_id` FK→`company.id`(`RESTRICT`) index; `expense_id` FK→`expense.id`(`CASCADE`) NOT NULL index; `storage_key` text NOT NULL (not exposed externally); `filename` text nullable (client original name, display only); `mime_type` text NOT NULL; `byte_size` Integer NOT NULL; `sha256` text nullable (placeholder for dedup); `creator_id` FK→`user.id`(`SET NULL`) nullable; timestamps.
- **`recurring_expense`**: `id`; `company_id` FK→`company.id`(`RESTRICT`) index; `name` text NOT NULL; template fields (`category_id` FK `SET NULL` + `category_name`, `supplier_name`, `vat_treatment_id` FK `RESTRICT` + snapshots, `vat_rate_id` FK `RESTRICT` + snapshots, `net_amount`/`vat_amount`/`gross_amount` `NUMERIC(18,3)`, `deductible`, `note`, same spec as expense); `frequency` `RecurringFrequency` (MONTHLY/QUARTERLY/YEARLY); `start_date` Date NOT NULL; `end_date` Date nullable; `max_occurrences` Integer nullable; `occurrences_generated` Integer NOT NULL server_default `0`; `next_run_date` Date NOT NULL index; `last_generated_at` DateTime nullable; `active` Boolean NOT NULL server_default `true`; `creator_id` FK→`user.id`(`SET NULL`); timestamps. Indexes: `ix_recurring_expense_company_id` / `ix_recurring_expense_next_run_date`.
- **Delete safety**: Delete `expense` → DB cascade deletes `expense_attachment` rows + service deletes disk files (red line 3 + D12); delete `expense_category`/`vat_rate`/`vat_treatment` behavior same as invoice/payment pattern (category `SET NULL` preserves name snapshot; rate/treatment `RESTRICT`); delete `recurring_expense` → generated expense `recurring_expense_id` `SET NULL` (historical records stay independent); delete `user` → `creator_id` `SET NULL`.
- **RLS**: Continue to reserve but not activate; service layer centralizes on current user's `company_id` (red line 2).

---

## Atomic Step List
> Each step = one atomic change (CI green = can merge to `main`); satisfies roadmap §5 DoD. **Each step self-contained**: gives clear files to mirror + invariants + required test coverage + blind review points. **`services/expense`/`recurring`/AI parsing etc. money-math/state logic must have unit tests.**
> Split intent: land "accounting data foundation + split/default/snapshot correct" first (independently unit-testable) → layer receipt storage → layer recurring generation → layer AI receipt fill → frontend + closure. AI / external model details concentrated in step 4.

### Step 1 · `expense` table + migration + `services/expense` CRUD + schemas
- **Goal**: Record one expense and see backend-calculated `gross`, resolved `deductible` default, and full snapshots.
- **Contract**: `ExpenseInput`/`ExpenseRead`/`ExpenseListItem`/`ExpenseListResponse`; `POST/GET /expenses`, `GET/PUT/DELETE /expenses/{id}`.
- **Implementation tasks**:
  - **Backend**:
    - `models/_enums.py`: No additions needed (`RecurringFrequency` deferred to step 3).
    - `models/expense.py`: `Expense` ORM (per "Data Model" above; `_MONEY=Numeric(18,3)` etc. mirrors `models/invoice.py`/`models/payment.py`); register in `models/__init__.py`. **Step 1's `recurring_expense_id` column: build as plain nullable UUID first, FK deferred until step 3 creates the target table** (or step 1 omits column, step 3 additive-adds it — implementer chooses; blind review only checks "doesn't break additive property").
    - `schemas/expense.py`: schemas as specified above (mirrors `schemas/invoice.py` Decimal/validation style).
    - `services/expense.py`: `create_expense`/`list_expenses`/`get_expense`/`update_expense`/`delete_expense` — split storage + `gross` calculation + `deductible` default resolution + full snapshots + single base currency + `vat_treatment.side==PURCHASE` guard (see "Money-Math Rules"); inject `company_id`; list filter/pagination/sort mirrors `services/payment.py::list_payments`. `delete_expense` disk file hook deferred to step 2 (no attachments in step 1).
    - `api/expenses.py`: Thin router (owner-only, `company_id` injected); register in `api/__init__.py`.
  - **Frontend**: Only `npm run codegen` (UI deferred to step 5).
- **Migration**: Create `expense` + indexes (one additive; don't touch existing tables).
- **Required test coverage**: `gross = net + vat` quantized to cents; `net×rate ≠ vat` still stores (D8 no equality check); `deductible` default resolution three tiers (category True/False/None→fallback True; explicit override); snapshots correct (delete category → `category_name` still there); SALE-side treatment rejected (`422`); non-base-currency rejected (`422`); `base_* = original`, `exchange_rate=1`; list filter (category/date/deductible/`is_draft`/`q`) + pagination `total` + sort; cross-company `404`; owner-only; negative amounts rejected.
- **Blind review points**: ① Split + `gross` + default + snapshot logic in `services/`, covered by unit tests (red line 1); ② **no `vat==net×rate` validation** (D8); ③ `vat_treatment.side==PURCHASE` guard exists; ④ `company_id` injected by service, no scattered `where company=` (red line 2); ⑤ amounts `Decimal`/`NUMERIC(18,3)`, persisted via `quantize_to_minor_unit` (D11); ⑥ single base currency (D10); ⑦ `text` columns correct (red line 10); ⑧ dict FK ondelete + snapshots match D7/D12; ⑨ contract + `schema.d.ts` no drift.
- **DoD**: See roadmap §5.

### Step 2 · Storage abstraction + receipt upload/download/delete (`expense_attachment`)
- **Goal**: Upload receipt image/PDF for one expense; preview, download, delete; delete expense cascades to clear attachments + disk files.
- **Contract**: `ExpenseAttachmentRead`/`ExpenseAttachmentListResponse`; `POST/GET /expenses/{id}/attachments`, `GET /attachments/{id}/content`, `DELETE /attachments/{id}`.
- **Implementation tasks**:
  - **Backend**:
    - `config.py`: Add `storage_root: str` (dev default `./var/storage`), `max_receipt_bytes: int` (default 10 MB).
    - `services/storage.py`: `Storage` abstraction + `LocalStorage` (see "Storage Abstraction" section; path traversal prevention, whitelist extension, magic-bytes/MIME double-check). Mirrors `services/assets.py` validation style.
    - `models/expense_attachment.py` + register; migration creates table (`CASCADE` to expense).
    - `schemas/expense.py`: Add attachment schemas (**don't expose `storage_key`/path**).
    - `services/expense.py`: `add_attachment` (multipart → validate → `storage.save` → persist metadata row), `list_attachments`, `get_attachment_content` (get this company's attachment → `storage.open` → stream), `delete_attachment` (delete row + `storage.delete`); and in `delete_expense` delete all disk files for that expense's attachments (D12).
    - `api/expenses.py`/`api/attachments.py`: Routes (`UploadFile`, mirrors M2 logo upload; owner-only, `company_id` injected).
  - **Frontend**: `npm run codegen`.
- **Migration**: Create `expense_attachment` + indexes (one additive).
- **Required test coverage**: Upload valid PNG/JPEG/WebP/PDF succeeds; metadata persists + disk write; non-whitelist MIME / over-size → `422`; spoofed extension / `..` path rejected (red line 7); `GET .../content` returns original MIME + inline; delete attachment clears DB + disk; **delete expense cascades to clear attachment rows + disk files**; cross-company attachment `404`; owner-only; `storage_key`/path not in any response.
- **Blind review points**: ① Receipts via disk storage abstraction, **not stuffed into `binary_asset`** (D2); ② path no traversal, filename untrusted from client (red line 7); ③ delete expense → attachment rows cascade + disk files deleted by service (red line 3 + D12); ④ `storage_key`/disk path not exposed; ⑤ storage interface shape reusable by M9 PDF (`save/open/delete/exists` + namespace); ⑥ contract no drift.
- **DoD**: See roadmap §5.

### Step 3 · Recurring expenses (`recurring_expense` + APScheduler second job)
- **Goal**: Create monthly/quarterly/yearly template; idempotently generate `is_draft=true` expenses at due time; pausable; can set limits; manual "run now" works.
- **Contract**: `RecurringExpenseInput`/`RecurringExpenseRead`/`RecurringExpenseListResponse`; `POST/GET /recurring-expenses`, `GET/PUT/DELETE /recurring-expenses/{id}`, `POST /recurring-expenses/{id}/run-now`.
- **Implementation tasks**:
  - **Backend**:
    - `models/_enums.py`: Add `RecurringFrequency` (MONTHLY/QUARTERLY/YEARLY).
    - `models/recurring_expense.py` + register; migration creates table + adds FK to `expense.recurring_expense_id` (if step 1 didn't create the column, additive add column + FK `SET NULL` here).
    - `schemas/expense.py` (or new `schemas/recurring_expense.py`): schemas as specified.
    - `services/recurring_expense.py`: CRUD + **pure function `compute_next_run(frequency, from_date)`** (monthly/quarterly/yearly step; **default use stdlib hand-calc** — add N months then use `calendar.monthrange` to clamp overflow day to month-end, e.g. 1/31 + 1 month → 2/28; **don't introduce new dependencies** unless implementer explicitly chooses `python-dateutil`. **Independently unit-testable**) + `generate_due_recurring_expenses_all(session)` (iterate `active` templates with `next_run_date <= today` and not over limit → clone `is_draft=true` expense [`recurring_expense_id` points to template; copy snapshot fields] → advance `next_run_date`/`occurrences_generated`/`last_generated_at` → if over limit/end-date → `active=false`; **idempotent**: generate once per period, advance cursor in transaction). Reuses step 1's expense persist logic, **don't re-implement money-math**.
    - `main.py`: Add second cron job `_generate_recurring_expenses_job` to `lifespan` scheduler (mirrors `_expire_quotes_job`: get session, call `generate_due_recurring_expenses_all`, `commit`/`rollback`, log); config adds `scheduler_recurring_expenses_hour` (default 2, offset from quote-expiry at hour 1).
    - `api/recurring_expenses.py`: CRUD + `run-now` (calls same generation function, scope to just that template).
  - **Frontend**: `npm run codegen`.
- **Migration**: Create `recurring_expense` + indexes + (if needed) `expense.recurring_expense_id` FK (additive).
- **Required test coverage**: `compute_next_run` monthly/quarterly/yearly step (includes cross-year, month-end clamp e.g. 1/31→2/28; pure function unit test); `generate_due_*` idempotent (run twice, same period not generated twice); at-due generates `is_draft=true` with correct fields/snapshots cloned, `recurring_expense_id` points back; pause (`active=false`) skips; limit (`max_occurrences` / `end_date`) reached → stops and sets `active=false`; `run-now` same behavior as scheduled; cross-company isolation; owner-only. **Integration test** mirrors `tests/test_quote_convert_reactivate_integration.py` style.
- **Blind review points**: ① Generated expense `is_draft=true` (D3); ② idempotent (cursor advanced in transaction, no duplicate); ③ money-math logic reuses step 1, no duplicate implementation; ④ scheduler job mirrors `_expire_quotes_job` transaction/exception handling; ⑤ delete template doesn't delete historical expenses (`recurring_expense_id` `SET NULL`); ⑥ `company_id` centralized; ⑦ contract no drift.
- **DoD**: See roadmap §5.

### Step 4 · ⭐ AI Receipt Smart Fill (`services/ai` OpenAI-compatible pipeline + AI settings + connectivity test)
- **Goal**: For one already-uploaded receipt (**image / PDF**; PDF backend-rasterized to image), call OpenAI-compatible Chat Completions vision model, return prefill fields (not persisted), manual confirm then step 1 persists; AI off/failure graceful fallback to hand-fill; settings page can **test** connectivity + multi-modal.
- **Contract**: `ExpenseAIPrefill`/`AiSettings`/`AiSettingsRead`/`AiSettingsUpdate`/`AiTestResult`; `POST /expenses/ai-extract`, `GET/PUT /settings/ai`, `POST /settings/ai/test`.
- **Implementation tasks**:
  - **Backend**:
    - `pyproject.toml`: **Promote `httpx` from dev group to main dependency**; add `pypdfium2` + `Pillow` (PDF rasterization, D16; pure wheel, no system packages). **Don't add `openai`/`anthropic`/`pdf2image`/`PyMuPDF`**.
    - `config.py`: Add `ai_base_url: str="https://api.openai.com/v1"`, `ai_api_key: str=""`, `ai_model: str=""`, `ai_enabled: bool=false`, `ai_pdf_max_pages: int=3`, `ai_pdf_render_scale: float` (or DPI) (env fallback).
    - `schemas/setting.py`: `SETTING_KEY_AI="ai"` + `AiSettings`/`AiSettingsRead`/`AiSettingsUpdate`/`AiTestResult` (**mirrors `SmtpSettings`/`SmtpSettingsRead`/`SmtpSettingsUpdate`** + `base_url`/`receipt_prompt` fields).
    - `services/ai.py`: `_get_ai_config` (DB→env fallback, mirrors `services/email.py::_get_smtp_config`); `DEFAULT_RECEIPT_PROMPT` + `_OUTPUT_CONTRACT` constants; `_attachment_to_images(...)` (`image/*` use directly; `application/pdf` → `pypdfium2` page-by-page render (limit `ai_pdf_max_pages`) → `Pillow` encode; other MIME → `422`); `httpx.AsyncClient` call wrapper (`POST {base_url}/chat/completions`, request/response custom small Pydantic models, **client injectable/mockable**, `base_url`/`model`/`prompt` all from config not hardcoded); `extract_from_attachment(...)` (get this company's attachment → AI off/no key → clear error → read storage file → `_attachment_to_images` → each image `image_url` + effective prompt → call Chat Completions → **fault-tolerant parse** → service best-effort match category → **return, don't persist**); `test_ai_config(cfg) -> AiTestResult` (built-in tiny image probe, see "AI Receipt Pipeline" section).
    - `api/settings.py`: Add `GET/PUT /settings/ai` + `POST /settings/ai/test` (owner-only, mirrors `/settings/smtp` + `/smtp/test`, **read-time masked**).
    - `api/expenses.py`: Add `POST /expenses/ai-extract`.
  - **Frontend**: `npm run codegen` (UI in step 5).
- **Migration**: None (AI settings use existing `setting` table key-value).
- **Required test coverage (all mock `httpx` via `respx`/`MockTransport`; never hit real network)**: Fake response → prefill field mapping + category name match correct; model returns JSON with code fence / missing fields / extra fields → fault-tolerant parse (all nullable, unknown keys ignored); connection/parse failure/timeout → `502` + recoverable signal; `enabled=false` or no key → clear error code (not 500); cross-company attachment → `404`; **PDF rasterization**: electronic PDF / multi-page PDF (verify page limit `ai_pdf_max_pages` enforced, each page one `image_url` part) / scanned (image-only) PDF all convert to images for model (use small PDF fixture, `pypdfium2` real render, no network); genuinely unsupported MIME (e.g. `text/csv`) → `422`; `AiSettingsRead` masked (`api_key_set` not plain; `base_url`/`receipt_prompt` returned); **key not in logs, not in any response body** (including `ExpenseAIPrefill`/`AiTestResult`); `PUT` omit `api_key` retains existing, empty string clears (mirrors SMTP); `receipt_prompt` empty → use `DEFAULT_RECEIPT_PROMPT`; non-empty → use user value; **both are appended with `_OUTPUT_CONTRACT`**; `test` endpoint: success → `{ok:true,multimodal:true}`; auth fail/model rejects image → corresponding false + detail.
- **Blind review points**: ① Credentials **mirror SMTP pattern**, read masked, env fallback (D1); ② **key never in logs / response** (grep `api_key`/`ai_api_key` for locations); ③ client = **`httpx` self-constructed OpenAI-compatible Chat Completions**, no `openai`/`anthropic`/`requests`, `base_url`/`model`/`prompt` from config not hardcoded, **injectable, unit tests no real network** (D6); ④ prompt = user value or default + **system-fixed appended output contract footer (non-editable by user)**, fault-tolerant parse (D15); ⑤ PDF rasterized by `pypdfium2` to image then `image_url`, page limit enforced, **no system poppler/PyMuPDF** (D16); ⑥ prefill **not persisted**; persist still goes through step 1 (D5); ⑦ failure/off graceful fallback (no bare 500); ⑧ genuinely unsupported MIME blocked at `422`; ⑨ treat receipt image as untrusted, parse has type/range validation (red line 7); ⑩ test probe uses built-in image, no user data sent (D14); ⑪ contract no drift.
- **DoD**: See roadmap §5.

### Step 5 · Frontend + Closure (i18n + UX + Deployment Self-Test Points)
- **Goal**: All expense flows visually operable; AI settings in gear panel; EN/ZH complete.
- **Contract**: Align with step 1–4 `schema.d.ts`.
- **Implementation tasks**:
  - **Backend**: Small fixes as needed (error codes / copy / sorting / docstrings).
  - **Frontend**:
    - `stores/expenses.ts` / `stores/recurringExpenses.ts`; expense list page (filters: category/date/deductible/draft/search + pagination); expense editor (category/supplier/net+VAT/rate/PURCHASE treatment/deductible; **amount display uses backend `gross`; frontend doesn't locally authority-calculate**, red line 1); receipt panel (upload + raw image `<img>`/PDF embed preview + download + delete); **"photograph receipt auto-fill" entry** (upload receipt first → call `ai-extract` → fill `ExpenseAIPrefill` back into editor → user confirms then saves; AI off/failure: button disabled or prompt falls back to hand-fill); recurring expense management page (CRUD + pause + "run now" + generated drafts show `is_draft` badge in expense list; one-click confirm to commit).
    - Gear settings panel add **AI category** (reuse M2.5 placeholder): fill `base_url` + `model` + API key (after write only shows `api_key_set`) + enable switch + **prompt textarea** (`receipt_prompt`; placeholder shows default when empty; "restore default" button) + **"test" button** (calls `POST /settings/ai/test`; shows connectivity + whether multi-modal + detail).
    - Note prod-build dynamic-prop button not dropping `@click` (memory `vue-loading-prop-vif-prod-bug`).
  - **Migration**: None.
  - **i18n**: `expenses.*`/`recurring.*`/`receipt.*`/`ai.*` into `en.json` + `zh.json`.
- **Test / self-check**: `npm run build`; `schema.d.ts` no drift. (No per-step manual walkthrough — see milestone-end self-test points below.)
- **Blind review points**: ① Frontend **doesn't locally authority-calculate**; `gross`/`base_*` from backend (red line 1); ② editor only sends raw inputs (no `gross`/snapshots/`base_*`); ③ AI key write-only on frontend, never shows plain (only `api_key_set`); `base_url`/`model` visible; ④ AI off/failure UI falls back to manual fill, doesn't freeze; ⑤ "test" button clearly shows `AiTestResult` (connectivity + multi-modal); ⑥ receipt preview doesn't expose storage path; ⑦ prod-build dynamic button doesn't drop `@click`; ⑧ i18n keys complete, both EN/ZH.
- **DoD**: See roadmap §5.

## Follow-On (Separate Round After M8 Main-Line Closure · Not Counted in M8 Acceptance · D4)
> **⭐ AI supplier price-list recognition → fill M4 product catalog**: Reuses step 4's `services/ai` vision pipeline; supplier price-list (image/PDF/Excel) → recognize → **SKU upsert** into `product` (override price on existing SKU, always create new if no SKU; see memory `product-sku-upsert`); feeds M6.5 costing. Excel parsing adds `openpyxl` as needed. When entering this round, JIT-supplement contract / tests / blind review points (same format as this doc).

## 🟢 Deployment Self-Test Points (Milestone Acceptance · Author Final Single Manual Walkthrough)
> No more per-step manual walkthroughs (automated tests + blind review handle those); this set is for **entire M8 main-line post-completion** author manual audit. Local: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build`; browser `http://localhost:${APP_HOST_PORT:-8000}`.

1. **Record one expense**: Fill category / supplier / net + input VAT / rate / PURCHASE treatment / deductible (default from category, overridable) → save; `gross = net + vat` to cents; snapshot fields correct.
2. **Deductible default**: Switch to a category with different `default_deductible`; new expense default changes accordingly; per-line override works.
3. **Receipt upload/preview/delete**: Upload one receipt image + one PDF → preview (image embed, PDF embed) + download; delete one attachment; **delete entire expense → attachment disk files disappear**.
4. **AI settings + test + prompt**: Gear panel AI category: fill `base_url`/`model`/key + enable → click "test" to actually probe once → shows "connected + model supports image input"; fill non-multimodal/wrong-key config → test clearly reports failure reason; change `receipt_prompt` and save → next extraction uses new prompt; clear → falls back to default.
5. **Photograph receipt auto-fill (image + PDF)**: Upload ① image receipt, ② electronic PDF, ③ scanned PDF → "auto-fill" all can prefill net/VAT/rate/supplier/date/category suggestion (PDF backend-rasterized) → edit and confirm then save (**not persisted before confirm**); multi-page PDF only takes first N pages; AI off / model failure → falls back to manual fill, doesn't freeze.
6. **Recurring expenses**: Create monthly template → "run now" (or wait for scheduled) generates one `is_draft=true` draft → confirm in expense list to commit; after pause no more generation; limit reached → auto-stops.
7. **Isolation / cascade / single currency**: Cross-company access to expense / attachment → `404`; non-base-currency amount rejected (`422`); delete template doesn't delete historical expenses.
8. **Settings security**: After AI key write, shows only "set" indicator; refresh doesn't show plain key; backend logs / responses don't contain key.
9. **CI four gates all green; `schema.d.ts` no drift; `docker build` passes.**

## Acceptance (Filled at Closure)
- **Completion Date**: 2026-06-14.
- **Acceptance**: 5-step main line (expense CRUD+split / storage receipts / recurring expenses / AI receipt fill / frontend closure) orchestrator mode step-by-step implementation; each step blind review + rework convergence; automated tests ruff + mypy --strict (88 files) + unit tests 599 (integration separate; test_expense_integration 42) + frontend build + `schema.d.ts` no drift — all green.
  Author final manual walkthrough on self-test points found batch of usability/recognition issues; singled out **walkthrough refinements** (all folded into commit `e7e723d`): ① deductible follows category link (`@update:value` fix; another category `default_deductible=NULL` is data issue not code); ② receipt storage switched to bind-mount host dir + uid 1000 container run for easy backup (`docker-compose.yml`); ③ AI connectivity probe image 1×1→64×64 (vision gateway rejects <8px; was causing false "not supported"); ④ inject current date at extraction time (fixes "future 2025" misread); ⑤ AI writes receipt summary into expense `note` (new `summary` field; **explanatory output follows UI language**; proper nouns/category names stay original); ⑥ select rate + enter net auto-calculates VAT (new `POST /expenses/calculate`; VAT still overridable); ⑦ prompt semantics changed from "custom replaces default" to "**default always resident + custom appended as additional instructions**" (settings label changed to Additional Instructions / 附加指令).
  - **Self-test points**: 1–5 manual pass (with above refinements re-tested); **6 recurring expenses** author not using now, defer walkthrough (deemed complete; retest later if chance); **7 isolation/cascade/single-currency** (cross-company `404`, non-base `422`, delete template keeps history) covered by integration tests; **8 settings security** (key write shows "set" only, logs/response no key) pass; **9 CI four gates + no drift + docker build** local DoD-equivalent all green; remote CI same gates await author final confirmation.
- **Known Deferred / Out of Scope**: AI supplier price-list → catalog (follow-on, separate round after M8 closure); input VAT reporting aggregation (2a/4a/4b/5b) → M10; partial deductible % → deferred; multi-currency / expense FX → deferred; supplier entity / customer hang → deferred; server thumbnails → not doing; bank import → vNext; **recurring self-test point 6 author hasn't manually walked** (doesn't affect acceptance; author retest on demand). (PDF receipt AI extraction **is included in M8** — rasterize to image, see D16.)
